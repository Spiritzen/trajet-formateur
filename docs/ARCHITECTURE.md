# Architecture (brouillon)
- Backend multi-modules (domain, persistence, service, web)
- Frontend React + Vite
- Postgres + MailHog via docker-compose
- Sécurité: JWT HttpOnly, rôles (ADMIN, GESTIONNAIRE, FORMATEUR, ECOLE)
